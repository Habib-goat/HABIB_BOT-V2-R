module.exports = {
  config: {
    name: "callWelcome",
    eventType: [
      // এখানে Call Event-এর নাম থাকবে
    ],
    version: "1.0.0",
    author: "Riyad"
  },

  onStart: async function ({ api, event, threadsData, usersData }) {
    // Call Join Event Handle হবে
  }
};
